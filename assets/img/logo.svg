<?xml version="1.0" encoding="UTF-8"?>
<!--
  Tau Intelligence — placeholder logo mark.
  Replace assets/img/logo.png with the official PNG when convenient;
  this SVG is used as a fallback so the site never renders broken.
-->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" width="64" height="64">
  <defs>
    <radialGradient id="g" cx="50%" cy="40%" r="60%">
      <stop offset="0%"  stop-color="#E9A766"/>
      <stop offset="60%" stop-color="#E8722C"/>
      <stop offset="100%" stop-color="#B8541C"/>
    </radialGradient>
  </defs>
  <rect width="64" height="64" rx="12" fill="#15130F"/>
  <text x="50%" y="56%" text-anchor="middle"
        font-family="'Cormorant Garamond', Georgia, serif"
        font-style="italic" font-weight="600" font-size="44"
        fill="url(#g)">τ</text>
</svg>
